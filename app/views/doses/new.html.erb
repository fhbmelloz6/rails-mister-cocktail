<p> <strong>From=> </strong> views doses new.html.erb </p>

<%= simple_form_for([@cocktail, @dose]) do |f| %>
  <%= f.error_notification %>
  <%= f.input :description %>
  <%= f.association :ingredient %>

  <%= f.submit "Crie um novo atributo" %>
<% end %>
