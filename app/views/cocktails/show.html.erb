<p> <strong>From=> </strong> show.html.erb <p>
<h2><%= @cocktail.name %></h2>

<p><% @cocktail.doses.each do |d| %>
  <%= d.description %> - <%= d.ingredient.name %>
<% end %>

<%= cl_image_tag @cocktail.photo, height: 230, width: 200, crop: :fill %></p>

<%= link_to "Delete this cocktail", cocktail_path(@cocktail), method: :delete, class: "btn btn-primary" %>
<%= link_to "Back to list of cocktails", cocktails_path, class: "btn btn-primary" %>


<%= link_to "FHM Create dose", new_cocktail_dose_path(@cocktail.id), class: "btn btn-primary" %>
